(()=>{const K='velush_cart';const cfg=window.VELUSH_CONFIG||{};const money=n=>`৳${Number(n||0).toLocaleString('en-BD')}`;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const read=()=>{try{return JSON.parse(localStorage.getItem(K)||'[]')}catch{return[]}};const write=v=>{localStorage.setItem(K,JSON.stringify(v));updateCartCount()};const updateCartCount=()=>document.querySelectorAll('[data-cart-count]').forEach(x=>x.textContent=read().reduce((a,i)=>a+i.qty,0));const toast=m=>{let t=document.getElementById('toast');if(!t){t=document.createElement('div');t.id='toast';t.className='toast';document.body.appendChild(t)}t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)};const addToCart=item=>{const a=read(),key=[item.productId,item.size,item.color].join('|');const found=a.find(x=>x.key===key);if(found)found.qty+=item.qty;else a.push({...item,key});write(a);toast('Added to bag')};const removeFromCart=key=>write(read().filter(x=>x.key!==key));const updateQty=(key,qty)=>{const a=read(),i=a.find(x=>x.key===key);if(i)i.qty=Math.max(1,Number(qty)||1);write(a)};const clearCart=()=>{localStorage.removeItem(K);updateCartCount()};const productBySlug=slug=>(window.VELUSH_PRODUCTS||window.VELUSH_SEED_PRODUCTS||[]).find(p=>p.slug===slug);window.VELUSH={cfg,money,esc,cart:read,saveCart:write,addToCart,removeFromCart,updateQty,clearCart,updateCartCount,toast,productBySlug}})();document.addEventListener('DOMContentLoaded',()=>VELUSH.updateCartCount());