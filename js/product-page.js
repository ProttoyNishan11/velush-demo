(()=>{function slug(){return document.body.dataset.productSlug||location.pathname.split('/').pop().replace(/^product-/,'').replace(/\.html$/,'')}function draw(){const p=VELUSH.productBySlug(slug()),root=document.querySelector('[data-product-root]');if(!p||!root)return;const imgs=p.images?.length?p.images:['assets/logo.jpeg'];const out=Number(p.stock)<=0;root.innerHTML=`<div><div class="gallery-main"><img id="mainProductImage" src="${VELUSH.esc(imgs[0])}" alt="${VELUSH.esc(p.name)}"></div><div class="thumbs">${imgs.map((x,i)=>`<button class="thumb ${i===0?'active':''}" type="button" data-src="${VELUSH.esc(x)}"><img src="${VELUSH.esc(x)}" alt="${VELUSH.esc(p.name)} view ${i+1}"></button>`).join('')}</div></div><div class="product-info"><div class="eyebrow">${VELUSH.esc(p.category)}</div><h1>${VELUSH.esc(p.name)}</h1><div class="product-price">${VELUSH.money(p.price)}</div><p class="muted">Color: ${VELUSH.esc(p.color)}</p><div class="option-block"><label>Size</label><div class="option-row">${(p.sizes||[]).map((s,i)=>`<button class="size-btn ${i===0?'selected':''}" type="button" data-size="${VELUSH.esc(s)}">${VELUSH.esc(s)}</button>`).join('')}</div></div><div class="option-block"><label>Quantity · ${out?'Out of stock':`${p.stock} available`}</label><div class="qty"><button type="button" data-dec aria-label="Decrease">−</button><input id="qty" value="1" min="1" max="${Math.max(1,p.stock)}" inputmode="numeric" ${out?'disabled':''}><button type="button" data-inc aria-label="Increase">+</button></div></div><button class="btn btn-dark btn-wide" id="addBtn" type="button" ${out?'disabled':''}>${out?'Out of stock':'Add to bag'}</button><div class="delivery-box"><strong>Delivery</strong><span>Inside Dhaka · ৳80</span><span>Outside Dhaka · ৳130</span><span>Free delivery on orders above ৳3,500</span></div><details open><summary>Product details</summary><p>${VELUSH.esc(p.description||'Please select an available size before adding this item to your bag.')}</p></details><details><summary>Return / Exchange</summary><p>The product must not be damaged for return/exchange eligibility.</p></details></div>`;let size=p.sizes?.[0]||'M';root.querySelectorAll('[data-size]').forEach(b=>b.onclick=()=>{size=b.dataset.size;root.querySelectorAll('[data-size]').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')});root.querySelectorAll('.thumb').forEach(b=>b.onclick=()=>{root.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));b.classList.add('active');root.querySelector('#mainProductImage').src=b.dataset.src});const q=root.querySelector('#qty');root.querySelector('[data-inc]').onclick=()=>{q.value=Math.min(Number(q.max||999),Number(q.value||1)+1)};root.querySelector('[data-dec]').onclick=()=>{q.value=Math.max(1,Number(q.value||1)-1)};root.querySelector('#addBtn').onclick=()=>{const qty=Math.min(Number(q.value||1),Number(p.stock));VELUSH.addToCart({productId:p.id,productSlug:p.slug,name:p.name,price:Number(p.price),color:p.color,size,qty,image:imgs[0]});location.href='cart.html'}}document.addEventListener('DOMContentLoaded',draw);window.addEventListener('velush:data-ready',draw)})();