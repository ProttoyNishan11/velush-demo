function renderCartPage(){const root=document.querySelector('[data-cart-root]');if(!root)return;const items=VELUSH.cart();if(!items.length){root.innerHTML='<div class="empty-state"><h2>Your bag is empty.</h2><a class="btn btn-dark" href="shop.html">Continue shopping</a></div>';return}let subtotal=0;const rows=items.map(i=>{subtotal+=i.price*i.qty;return `<div class="cart-row"><img src="${VELUSH.esc(i.image||'assets/logo.jpeg')}" alt="${VELUSH.esc(i.name)}"><div><h3>${VELUSH.esc(i.name)}</h3><p>Size ${VELUSH.esc(i.size)} · ${VELUSH.esc(i.color)}</p><div class="qty"><button type="button" data-key="${VELUSH.esc(i.key)}" data-dec>−</button><span style="display:inline-flex;width:42px;height:42px;align-items:center;justify-content:center">${i.qty}</span><button type="button" data-key="${VELUSH.esc(i.key)}" data-inc>+</button></div></div><strong>${VELUSH.money(i.price*i.qty)}</strong><button class="remove" data-remove="${VELUSH.esc(i.key)}" type="button">Remove</button></div>`}).join('');root.innerHTML=`<div class="cart-layout"><div>${rows}</div><aside class="summary-card"><h3>Summary</h3><div class="summary-row"><span>Subtotal</span><strong>${VELUSH.money(subtotal)}</strong></div><div class="summary-row"><span>Delivery</span><span>Calculated at checkout</span></div><div class="summary-row summary-total"><span>Total</span><strong>${VELUSH.money(subtotal)}</strong></div><p class="muted" style="font-size:.85rem">Free delivery applies above ৳3,500.</p><a class="btn btn-dark btn-wide" href="checkout.html">Checkout</a></aside></div>`;root.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{VELUSH.removeFromCart(b.dataset.remove);renderCartPage()});root.querySelectorAll('[data-inc]').forEach(b=>b.onclick=()=>{const i=VELUSH.cart().find(x=>x.key===b.dataset.key);VELUSH.updateQty(b.dataset.key,i.qty+1);renderCartPage()});root.querySelectorAll('[data-dec]').forEach(b=>b.onclick=()=>{const i=VELUSH.cart().find(x=>x.key===b.dataset.key);VELUSH.updateQty(b.dataset.key,Math.max(1,i.qty-1));renderCartPage()})}document.addEventListener('DOMContentLoaded',renderCartPage);