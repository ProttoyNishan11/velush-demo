(()=>{const input=document.getElementById('searchInput'),btn=document.getElementById('searchBtn'),root=document.getElementById('searchResults');function go(){const q=input.value.trim().toLowerCase();const list=(window.VELUSH_PRODUCTS||[]).filter(p=>!q||[p.name,p.category,p.color].join(' ').toLowerCase().includes(q));root.innerHTML=list.map(p=>`<article class="product-card"><a href="product-${VELUSH.esc(p.slug)}.html"><div class="product-image"><img src="${VELUSH.esc(p.images?.[0]||'assets/logo.jpeg')}" alt="${VELUSH.esc(p.name)}" loading="lazy"></div><div class="product-meta"><div><h3>${VELUSH.esc(p.name)}</h3><span>${VELUSH.esc(p.category)} · ${VELUSH.esc(p.color)}</span></div><strong>${VELUSH.money(p.price)}</strong></div></a></article>`).join('')||'<div class="notice" style="grid-column:1/-1">No matching products.</div>'}btn.onclick=go;input.oninput=go;window.addEventListener('velush:data-ready',go);document.addEventListener('DOMContentLoaded',go)})();