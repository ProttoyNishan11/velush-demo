const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const body=await req.json();const method=String(body.payment_method||'');if(!['bkash','nagad','online'].includes(method))throw new Error('Unsupported payment method');return new Response(JSON.stringify({status:'not_configured',message:`${method} payment provider integration is a server-side boundary only. Merchant credentials/provider API implementation are required before any payment can be taken.`}),{status:501,headers:{...cors,'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({status:'error',message:e?.message||'Payment initialization failed'}),{status:400,headers:{...cors,'Content-Type':'application/json'}})}});